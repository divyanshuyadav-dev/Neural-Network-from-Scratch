{"metadata":{"kernelspec":{"language":"python","display_name":"Python 3","name":"python3"},"language_info":{"name":"python","version":"3.12.12","mimetype":"text/x-python","codemirror_mode":{"name":"ipython","version":3},"pygments_lexer":"ipython3","nbconvert_exporter":"python","file_extension":".py"},"kaggle":{"accelerator":"none","dataSources":[],"dockerImageVersionId":31234,"isInternetEnabled":true,"language":"python","sourceType":"notebook","isGpuEnabled":false}},"nbformat_minor":4,"nbformat":4,"cells":[{"cell_type":"code","source":"# This Python 3 environment comes with many helpful analytics libraries installed\n# It is defined by the kaggle/python Docker image: https://github.com/kaggle/docker-python\n# For example, here's several helpful packages to load\n\nimport numpy as np # linear algebra\nimport pandas as pd # data processing, CSV file I/O (e.g. pd.read_csv)\nimport math\n\n# Input data files are available in the read-only \"../input/\" directory\n# For example, running this (by clicking run or pressing Shift+Enter) will list all files under the input directory\n\n# import os\n# for dirname, _, filenames in os.walk('/kaggle/input'):\n#     for filename in filenames:\n#         print(os.path.join(dirname, filename))\n\n# You can write up to 20GB to the current directory (/kaggle/working/) that gets preserved as output when you create a version using \"Save & Run All\" \n# You can also write temporary files to /kaggle/temp/, but they won't be saved outside of the current session","metadata":{"_uuid":"8f2839f25d086af736a60e9eeb907d3b93b6e0e5","_cell_guid":"b1076dfc-b9ad-4769-8c92-a6c4dae69d19","trusted":true,"execution":{"iopub.status.busy":"2026-01-23T07:58:16.889438Z","iopub.execute_input":"2026-01-23T07:58:16.890396Z","iopub.status.idle":"2026-01-23T07:58:18.543399Z","shell.execute_reply.started":"2026-01-23T07:58:16.890358Z","shell.execute_reply":"2026-01-23T07:58:18.542363Z"}},"outputs":[],"execution_count":1},{"cell_type":"markdown","source":"**<h3 ><center>TODO: complete verbosity of logs</center></h3>**","metadata":{}},{"cell_type":"markdown","source":"## Convolution Layer\n### Code:","metadata":{}},{"cell_type":"code","source":"class Convolution_Layer:\n\n    def __init__(self, no_of_kernels, height_of_kernel, width_of_kernel, stride=1, padding=0, verbose=False):\n        self.x = None\n        self.k = None\n        self.b = None\n        self.c_y = no_of_kernels \n        self.h_k = height_of_kernel\n        self.w_k = width_of_kernel\n        self.stride = stride\n        self.pad = padding\n        self.verbose = verbose\n\n    def forward(self, x):\n        if len(x.shape) != 4:\n            raise Exception(\"I expect 4-D input, recheck your input dimensions\")\n            \n        # padding (assuming equal padding accross all side on both dimension)\n        if(self.pad > 0):\n            x = np.pad(x, pad_width = ( (0,0), (0,0) , (self.pad,self.pad), (self.pad, self.pad) ) )\n\n        # set or extract dimesion sizes\n        batch_size = x.shape[0]\n        c_x, h_x, w_x = x.shape[1], x.shape[2], x.shape[3]\n        # CHECK: h_y and w_y should be Integers\n        if ((h_x - self.h_k) % self.stride != 0) or ((w_x - self.w_k) % self.stride != 0):\n            raise Exception(\"Fractional ouput size calculated!, maybe your Stride or Pad settings are bad\")\n        h_y, w_y = int((h_x - self.h_k)/self.stride + 1), int((w_x - self.w_k)/self.stride + 1)\n        \n        # initalize other feild variables (IMPORTANT for backprop)            \n        self.x = x\n        if self.k is None: # filter/kernel\n            self.k = np.ones( (self.c_y, c_x, self.h_k, self.w_k) ) # TODO: use He/Xavier initialiser\n        if self.b is None: # bias\n            self.b = np.zeros( (self.c_y,) )\n            \n        # output activation map\n        y  = np.zeros( (batch_size, self.c_y, h_y, w_y) )\n        \n        # convolution loop\n        for b_idx in range(batch_size): # for each batch\n            for cy_idx in range(self.c_y): # for each kernel OR output channel\n                for hy_idx in range (h_y): # for each output row\n                    for wy_idx in range(w_y): # for each output column\n                        z = 0 # weighted sum (w1x1 + w2x2 + ... + wNxN)\n                        for cx_idx in range(c_x): # for each input channel\n                            for hk_idx in range(self.h_k): # for each kernel row\n                                for wk_idx in range(self.w_k): # for each kernel column\n                                    z += self.k[cy_idx, cx_idx, hk_idx, wk_idx] * x[b_idx, cx_idx, hk_idx + hy_idx*self.stride, wk_idx + wy_idx*self.stride]\n                        y[b_idx, cy_idx, hy_idx, wy_idx] = z + self.b[cy_idx]\n        # return activation map\n        return y\n\n    def backward(self, dy): #TODO: remove eta later, updation is done only in optimiser\n        # CHECK: validate the backprop input\n        if len(dy.shape) != 4:\n            raise Exception(\"Backprop expects 4-D input!\")\n        # the gradient output channel count shouls be equal to output channel count\n        if dy.shape[1] != self.c_y:\n            raise Exception(\"The channel size of backward-y is inconsistent with forward-y\")\n            \n        # extract shapes\n        batch_size = dy.shape[0]\n        c_y, h_y, w_y =  dy.shape[1], dy.shape[2], dy.shape[3]\n        c_x, h_x, w_x = self.x.shape[1], self.x.shape[2], self.x.shape[3]\n\n        # calculate bias gardient TODO: use numpy broadcast to simplyfy the update\n        db = np.zeros( (self.b.shape) ) #gradient shape same as bias shape\n        for cy_idx in range(c_y): # for each bias or output channel\n            for b_idx in range(batch_size): # for each batch\n                for hy_idx in range(h_y): # for each output row\n                    for wy_idx in range(w_y): # fpr each output column\n                        db[cy_idx] += dy[b_idx, cy_idx, hy_idx, wy_idx]\n        # update bias, TODO: do it inoptimiser instead\n        # self.b -= eta * db\n\n        # calculate kernel gardient\n        dk = np.zeros(self.k.shape) #gradient shape same as kernel shape\n        for cy_idx in range(c_y): # for each ouput channel or each kernel\n            for cx_idx in range(c_x): # for each input channel or each kernel channel\n                for hk_idx in range(self.h_k): # for each kernal row\n                    for wk_idx in range(self.w_k): #for each kernel column\n                        grad_sum = 0\n                        for b_idx in range(batch_size): # for each batch\n                            for hy_idx in range(h_y): # for each output row\n                                for wy_idx in range(w_y): # for each output column\n                                    grad_sum += dy[b_idx, cy_idx, hy_idx, wy_idx] * self.x[b_idx, cx_idx, (hk_idx+hy_idx*self.stride), (wk_idx+wy_idx*self.stride)]\n                        dk[cy_idx, cx_idx, hk_idx, wk_idx] = grad_sum\n        #update kernel, TODO: do it in optimiser instead\n        # self.k -= eta * dk\n        \n        #calculate grad-x (padded)\n        dx_pad = np.zeros(self.x.shape)\n        for b_idx in range(batch_size): # for each batch\n            for cx_idx in range(c_x): # for each input channel\n                for hx_idx in range(h_x): # for each input row\n                    for wx_idx in range(w_x): # for each input column\n                        grad_sum = 0\n                        for cy_idx in range(c_y): # for each ouput channel\n                            for hy_idx in range(h_y): #for each output row\n                                i = hx_idx - hy_idx * self.stride # kernel row index\n                                if i < 0 or i >= self.h_k: # skip for invalid kernel index\n                                    continue\n                                for wy_idx in range(w_y): # for each ouput column\n                                    j = wx_idx - wy_idx * self.stride # kernel column index\n                                    if j < 0 or j >= self.w_k:# skip invalid kernel index\n                                        continue\n                                    grad_sum += dy[b_idx, cy_idx, hy_idx, wy_idx] * self.k[cy_idx, cx_idx, i, j]\n                        dx_pad[b_idx, cx_idx, hx_idx, wx_idx] = grad_sum # padded x gradient\n                        \n        # unpad the x\n        dx = np.zeros( (batch_size, c_x, h_x - 2 * self.pad, w_x - 2 * self.pad) )\n        dx = dx_pad[:, :, self.pad : h_x - self.pad, self.pad : w_x - self.pad]\n        \n        # return gradients \n        return dx, dk, db\n        ","metadata":{"trusted":true,"execution":{"iopub.status.busy":"2026-01-23T07:58:18.545623Z","iopub.execute_input":"2026-01-23T07:58:18.546084Z","iopub.status.idle":"2026-01-23T07:58:18.570547Z","shell.execute_reply.started":"2026-01-23T07:58:18.546053Z","shell.execute_reply":"2026-01-23T07:58:18.569380Z"}},"outputs":[],"execution_count":2},{"cell_type":"markdown","source":"### Forward/Backward Pass:","metadata":{}},{"cell_type":"code","source":"\nconv = Convolution_Layer(2,7,7, stride=1, padding=3)\n\nx = np.ones((1,1,4,4), dtype = np.float64)\ny = conv.forward(x)\nprint(y, y.shape)\n\nprint (\"--\"*10)\n\ndy=y\ndx, dk, db = conv.backward(dy)\nprint(dx, dx.shape)\n","metadata":{"trusted":true,"execution":{"iopub.status.busy":"2026-01-23T07:58:18.571664Z","iopub.execute_input":"2026-01-23T07:58:18.572018Z","iopub.status.idle":"2026-01-23T07:58:18.612132Z","shell.execute_reply.started":"2026-01-23T07:58:18.571990Z","shell.execute_reply":"2026-01-23T07:58:18.610992Z"}},"outputs":[{"name":"stdout","text":"[[[[16. 16. 16. 16.]\n   [16. 16. 16. 16.]\n   [16. 16. 16. 16.]\n   [16. 16. 16. 16.]]\n\n  [[16. 16. 16. 16.]\n   [16. 16. 16. 16.]\n   [16. 16. 16. 16.]\n   [16. 16. 16. 16.]]]] (1, 2, 4, 4)\n--------------------\n[[[[512. 512. 512. 512.]\n   [512. 512. 512. 512.]\n   [512. 512. 512. 512.]\n   [512. 512. 512. 512.]]]] (1, 1, 4, 4)\n","output_type":"stream"}],"execution_count":3},{"cell_type":"markdown","source":"### Gradients Test:","metadata":{}},{"cell_type":"code","source":"#TODO: test the gradients\nconv = Convolution_Layer(2,4,4)\n\nx = np.ones((1,1,7,7))\ny = conv.forward(x)\n#suppose a simple loss function such that...\ndef loss(y):\n    return np.sum(y)\n#and....\nw = conv.k[0,0,0,0]\n# we can say Loss is a function of wieights i.e.\n# loss = J(w)\n\n#compute the small change in loss wrt small change in wieght i.e. d(loss)/d(w): numerical gradient of loss wrt w\n# let small change be eps\neps = 1e-5\n\n#we know d(J(w))/d(w) = ( J(w+eps)-J(w-eps) ) / ( eps-(-eps) ) --->derivative by first principle\nconv.k[0,0,0,0] = w + eps\n# loss_pos_eps = np.sum()","metadata":{"trusted":true,"execution":{"iopub.status.busy":"2026-01-23T07:58:18.614987Z","iopub.execute_input":"2026-01-23T07:58:18.615406Z","iopub.status.idle":"2026-01-23T07:58:18.633679Z","shell.execute_reply.started":"2026-01-23T07:58:18.615369Z","shell.execute_reply":"2026-01-23T07:58:18.632381Z"}},"outputs":[],"execution_count":4},{"cell_type":"markdown","source":"## Linear Layer\n\n### Code:","metadata":{}},{"cell_type":"code","source":"class Linear_Layer:\n\n    def __init__(self, number_of_nodes,verbose=False):\n        self.c = number_of_nodes #ouput feature size/number of classes\n        self.w = None\n        self.b = None\n        self.x = None\n        self.verbose = verbose\n\n    def forward(self, x):\n        # dimension check\n        if len(x.shape) != 2:\n            raise Exception(\"I expect 2-D input, recheck your input dimensions\")\n\n        batch_size = x.shape[0]\n        # input feature size\n        f = x.shape[1]\n\n        # save input for backprop\n        self.x = x\n        # weight init; TODO:use HE/Xavier Initisalizers\n        if self.w is None:\n            self.w = np.ones( (f, self.c) )\n        if self.b is None:\n            self.b = np.zeros( (self.c,) )\n\n        #output\n        y = np.zeros( (batch_size, self.c) )\n\n        # y = wx + b\n        for b_idx in range(batch_size): # for each batch\n            for c_idx in range(self.c): # for each output class\n                z = 0 #wieghted sum value\n                for f_idx in range(f): # for each input feature\n                    z += x[b_idx, f_idx] * self.w[f_idx, c_idx]\n                y[b_idx, c_idx] = z + self.b[c_idx]\n        # return the output feature\n        return y\n\n    def backward(self, dy):\n        # demnsion check\n        if len(dy.shape) != 2:\n            raise Exception(\"Backprop expects 2d input\")\n        if dy.shape[1] != self.c:\n            raise Exception(\"The output feature size of backward-y is mismatch with forward-y\")\n        if dy.shape[0]!= self.x.shape[0]: #if batch of forward and backward mismacth\n            raise Exception(\"forward batch size and backward batch size are mismatched\")\n\n        batch_size = dy.shape[0]\n        f = self.x.shape[1]\n\n        # compute bias gradients\n        db = np.zeros( (self.c,) )\n        for c_idx in range(self.c): # for each output class/feature\n            grad_sum = 0\n            for b_idx in range(batch_size): # for each batch\n                grad_sum += dy[b_idx, c_idx]\n            db[c_idx] = grad_sum\n        # update bias TODO:do it in optimiser instead\n        # self.b -= eta * db\n\n        # compute weight gradients\n        dw = np.zeros( (f, self.c) )\n        for f_idx in range(f): # for each input feature\n            for c_idx in range(self.c): # for each output class/feature\n                grad_sum = 0\n                for b_idx in range(batch_size): # for each bacth\n                    grad_sum += self.x[b_idx, f_idx]  * dy[b_idx, c_idx]\n                dw[f_idx, c_idx] = grad_sum\n        # update weight TODO:do it in optimiser instead\n        # self.w -= eta * dw\n\n        # compute x gardient\n        dx = np.zeros(self.x.shape)\n        for b_idx in range(batch_size): # for each batch\n            for f_idx in range(f): # for each input feature\n                grad_sum = 0\n                for c_idx in range(self.c): # for each output class/ feature\n                    grad_sum += self.w[f_idx, c_idx] * dy[b_idx, c_idx]\n                dx[b_idx, f_idx] = grad_sum\n\n        return dx, dw, db\n        ","metadata":{"trusted":true,"execution":{"iopub.status.busy":"2026-01-23T07:58:18.634936Z","iopub.execute_input":"2026-01-23T07:58:18.635321Z","iopub.status.idle":"2026-01-23T07:58:18.656662Z","shell.execute_reply.started":"2026-01-23T07:58:18.635283Z","shell.execute_reply":"2026-01-23T07:58:18.655715Z"}},"outputs":[],"execution_count":5},{"cell_type":"markdown","source":"### Forward/Backward Pass:","metadata":{}},{"cell_type":"code","source":"lin = Linear_Layer(1)\n\nx = np.zeros((1,4))\n\ny = lin.forward(x)\nprint(y)","metadata":{"trusted":true,"execution":{"iopub.status.busy":"2026-01-23T07:58:18.657702Z","iopub.execute_input":"2026-01-23T07:58:18.658034Z","iopub.status.idle":"2026-01-23T07:58:18.681687Z","shell.execute_reply.started":"2026-01-23T07:58:18.658000Z","shell.execute_reply":"2026-01-23T07:58:18.680705Z"}},"outputs":[{"name":"stdout","text":"[[0.]]\n","output_type":"stream"}],"execution_count":6},{"cell_type":"markdown","source":"### Gradients Test:","metadata":{}},{"cell_type":"code","source":"# TODO: test the gardient using numerical and analytical gardient comparison","metadata":{"trusted":true,"execution":{"iopub.status.busy":"2026-01-23T07:58:18.683146Z","iopub.execute_input":"2026-01-23T07:58:18.683897Z","iopub.status.idle":"2026-01-23T07:58:18.702621Z","shell.execute_reply.started":"2026-01-23T07:58:18.683866Z","shell.execute_reply":"2026-01-23T07:58:18.701271Z"}},"outputs":[],"execution_count":7},{"cell_type":"markdown","source":"## Non_Linear Layer\n### Code:","metadata":{}},{"cell_type":"code","source":"class Non_Linear_Layer:\n\n    def __init__(self, activation, verbose=False):\n        self.activation = activation\n        self.verbose = verbose\n        self.x = None\n\n    def forward(self, x):\n        # shape check\n        if len(x.shape) != 2:\n            raise Exception(f\"2-D shape expected! recieved:{len(x.shape)}-D\")\n\n        batch_size = x.shape[0]\n        f = x.shape[1]\n\n        # save input for backprop\n        self.x = x\n        # output decalration\n        y = np.zeros( (batch_size, f) )\n\n        # RELU\n        if self.activation == 'relu':\n            for b_idx in range(batch_size): # for each batch\n                for f_idx in range(f): # for each input feature\n                    y[b_idx, f_idx] = max(0, x[b_idx, f_idx])\n        else:\n            raise Exception(\"Unkonwn activation\")\n                    \n        # return the ouput\n        return y\n\n    def backward(self, dy):\n        # shape checks\n        if len(dy.shape) != 2:\n            raise Exception(f\"Bakcprop expects 2-D input, recieved:{len(dy.shape)}-D\")\n        if dy.shape[0] != self.x.shape[0]: # batch mismatch\n            raise Exception(f\"forward batch size and backward batch size are mismatched, expected:{self.x.shape[0]}, recieved{dy.shape[0]}\")\n        if dy.shape[1] != self.x.shape[1]: # feature size mismatch\n            raise Exception(f\"forward feature size and backward feature size are mismatched, expected:{self.x.shape[1]}, recieved{dy.shape[1]}\")\n        \n        batch_size = dy.shape[0]\n        f = dy.shape[1]\n\n        # output gardient to return\n        dx = np.zeros(self.x.shape)\n\n        # RELU\n        if self.activation == 'relu':\n            for b_idx in range(batch_size): # for each batch\n                for f_idx in range(f): # for each input/output feature\n                    dx[b_idx, f_idx] = dy[b_idx, f_idx] if self.x[b_idx, f_idx] > 0 else 0\n        else:\n            raise Exception(\"Unknown activation\")\n\n        return dx","metadata":{"trusted":true,"execution":{"iopub.status.busy":"2026-01-23T07:58:18.703735Z","iopub.execute_input":"2026-01-23T07:58:18.704057Z","iopub.status.idle":"2026-01-23T07:58:18.727885Z","shell.execute_reply.started":"2026-01-23T07:58:18.704016Z","shell.execute_reply":"2026-01-23T07:58:18.726616Z"}},"outputs":[],"execution_count":8},{"cell_type":"markdown","source":"### Forward/Backward Pass:","metadata":{}},{"cell_type":"code","source":"nlayer = Non_Linear_Layer(activation = 'relu')\nx = np.array(\n    [\n        [1,2,-2,1]\n    ],\ndtype=np.float64)\ny = nlayer.forward(x)\nprint(y)\n\n","metadata":{"trusted":true,"execution":{"iopub.status.busy":"2026-01-23T07:58:18.729335Z","iopub.execute_input":"2026-01-23T07:58:18.729719Z","iopub.status.idle":"2026-01-23T07:58:18.751406Z","shell.execute_reply.started":"2026-01-23T07:58:18.729683Z","shell.execute_reply":"2026-01-23T07:58:18.750030Z"}},"outputs":[{"name":"stdout","text":"[[1. 2. 0. 1.]]\n","output_type":"stream"}],"execution_count":9},{"cell_type":"markdown","source":"### Gradient Test:","metadata":{}},{"cell_type":"code","source":"#TODO: do the trivial gradient test","metadata":{"trusted":true,"execution":{"iopub.status.busy":"2026-01-23T07:58:18.754645Z","iopub.execute_input":"2026-01-23T07:58:18.754995Z","iopub.status.idle":"2026-01-23T07:58:18.774703Z","shell.execute_reply.started":"2026-01-23T07:58:18.754968Z","shell.execute_reply":"2026-01-23T07:58:18.773490Z"}},"outputs":[],"execution_count":10},{"cell_type":"markdown","source":"## Cross Entropy Loss\n### Code:","metadata":{}},{"cell_type":"code","source":"class Cross_Entropy_Loss: #TODO add regularisation\n\n    def __init__(self):\n        self.x = None\n        self.t = None\n    \n    # computes loss score per batch\n    def forward(self, x, t): # x is raw class scores & t is one hot encoded labels as array\n        # shape check\n        if len(x.shape) != 2:\n            raise Exception(f\"Invalid input shape, expected {2}-D, recieved {len(x.shape)}-D\")\n        if len(t.shape) != 2:\n            raise Exception(f\"Invalid input shape, expected {2}-D, recieved {len(x.shape)}-D\")\n\n        if x.shape[0] != t.shape[0]:\n            raise Exception(\"batch size of class scores and truth labels mismatched\")\n        if x.shape[1] != t.shape[1]:\n            raise Exception(\"no of classes of scores from model and truth labels are mismatched\")\n\n        batch_size = x.shape[0]\n        c = x.shape[1] # no of classes (length of class scores per batch)\n\n        self.x = x\n        self.t = t\n        loss = np.zeros((batch_size,)) #loss is defined per batch\n\n        # prepare the sum of exponents of the scores (the denominator in softmax)\n        sum_of_exp_of_scores = np.zeros((batch_size,))\n        for b in range(batch_size): # for each batch\n            for i in range(c): # for each class\n                sum_of_exp_of_scores[b] += math.exp(x[b, i])\n\n        for b_idx in range(batch_size): #for each batch\n            loss_sum = 0\n            for c_idx in range(c): # for each class\n                loss_sum += -1 * math.log( math.exp(x[b_idx, c_idx]) / sum_of_exp_of_scores[b_idx] ) * t[b_idx, c_idx]\n            loss[b_idx] = loss_sum\n\n        return loss\n\n\n    #return loss gradient wrt input (class scores)\n    def backward(self):\n        batch_size = self.x.shape[0]\n        c = self.x.shape[1] #no of classes\n\n        dx = np.zeros( (self.x.shape) )\n\n         # prepare the sum of exponents of the scores (the denominator in softmax)\n        sum_of_exp_of_scores = np.zeros((batch_size,))\n        for b in range(batch_size): # for each batch\n            for i in range(c): # for each class\n                sum_of_exp_of_scores[b] += math.exp(self.x[b, i])\n\n        # compute gradient !! BUG !!\n        for b_idx in range(batch_size): #for each batch\n            for c_idx in range(c): # for each class\n                dx[b_idx, c_idx] += self.t[b_idx, c_idx] * ( math.exp(self.x[b_idx, c_idx]) / sum_of_exp_of_scores[b_idx] - 1 ) #wrong computation !!!!!\n\n        return dx","metadata":{"trusted":true,"execution":{"iopub.status.busy":"2026-01-23T07:58:18.776311Z","iopub.execute_input":"2026-01-23T07:58:18.776722Z","iopub.status.idle":"2026-01-23T07:58:18.796629Z","shell.execute_reply.started":"2026-01-23T07:58:18.776681Z","shell.execute_reply":"2026-01-23T07:58:18.795368Z"}},"outputs":[],"execution_count":11},{"cell_type":"code","source":"math.exp(709)","metadata":{"trusted":true,"execution":{"iopub.status.busy":"2026-01-23T07:58:18.797709Z","iopub.execute_input":"2026-01-23T07:58:18.797993Z","iopub.status.idle":"2026-01-23T07:58:18.823648Z","shell.execute_reply.started":"2026-01-23T07:58:18.797967Z","shell.execute_reply":"2026-01-23T07:58:18.822533Z"}},"outputs":[{"execution_count":12,"output_type":"execute_result","data":{"text/plain":"8.218407461554972e+307"},"metadata":{}}],"execution_count":12}]}